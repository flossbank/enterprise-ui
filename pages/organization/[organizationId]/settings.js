import PageWrapper from '../../../components/common/pageWrapper'
import OrgSettingsSection from '../../../components/organization/settings/settingsSection'

const Settings = () => (
  <PageWrapper title='Settings'>
    <OrgSettingsSection />
  </PageWrapper>
)

export default Settings
