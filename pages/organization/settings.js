import PageWrapper from '../../components/common/pageWrapper'
import UserSettingsSection from '../../components/organization/settings/settingsSection'

const Settings = () => (
  <PageWrapper title='Settings'>
    <UserSettingsSection />
  </PageWrapper>
)

export default Settings
